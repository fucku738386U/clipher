services:
  - type: web
    name: clipher
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python a.py
    envVars:
      - key: OPENROUTER_API_KEY
        sync: false
      - key: PYTHON_VERSION
        value: 3.10
